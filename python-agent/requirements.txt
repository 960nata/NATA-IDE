requests
python-docx
fpdf2
python-pptx

# Image generation (Stable Diffusion) — install terpisah via setup_image_gen.sh
# karena torch itu besar (~2GB download). Jangan install manual kalau belum ada.
diffusers>=0.28.0
transformers>=4.36.0
accelerate>=0.24.0
torch>=2.0.0
Pillow>=9.0.0
